services:
  - type: web
    name: ingea
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev && node tools/fetch-img.js
    startCommand: node server.js
    envVars:
      - key: INGEA_ADMIN_USER
        sync: false
      - key: INGEA_ADMIN_PASS
        sync: false
